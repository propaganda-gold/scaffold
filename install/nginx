BASE_PATH=/var/www
WEB_SITE=$1
NGINX_PATH=${BASE_PATH}/${WEB_SITE}
ABSOLUTE_GIT_PATH=${PWD}/nginx/html/${WEB_SITE}

sudo mkdir ${NGINX_PATH}
sudo ln -s ${ABSOLUTE_GIT_PATH} ${NGINX_PATH}/html 

ABSOLUTE_GIT_PATH=${PWD}/nginx/etc/${WEB_SITE}
sudo ln -s ${ABSOLUTE_GIT_PATH} /etc/nginx/sites-enabled/
